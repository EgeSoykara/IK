# UI/UX Regression Checklist

Run this checklist after UI/UX implementation, visual polish, final-touch, or regression-fix work.

## Evidence: 2026-08-06 Audit Search, Top Filters, Retrospective Leave, and Cancellation
- `/AuditLogs` rendered no table before an explicit search and explained that the full history is not loaded automatically. An empty search kept the dialog open with `Arama yapmak için en az bir filtre seçin.`; entering `admin` then produced 25 server-paged rows and one active user-filter chip. The browser interaction exposed and closed a delayed text-binding defect by making both free-text criteria immediate.
- Audit action labels and stored key/value details were visually checked in plain Turkish. Integer days rendered as `30 gün` rather than `30,0`; transitions rendered as `10 gün → 17 gün`; boolean and status values used `Evet/Hayır` and ordinary workflow terms; login records rendered `Görünen ad: Yönetici` rather than the stored technical key.
- The single shared `Filtrele` action on `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, `/LeaveApprovals`, `/LeaveCarryOverWarnings`, and `/AuditLogs` is directly below the page header. Browser geometry on representative routes measured header bottom `185px` and filter top `205px`; filter controls were absent from MudTable/Excel toolbars while Excel controls stayed in their existing toolbar.
- `/LeaveRequests` displayed `İptal Durumu` and owner/editor actions for direct pending cancellation or approved-leave cancellation request. The create popup exposed the explicit `Geriye dönük izin talebi` checkbox without leaving `/LeaveRequests`. The cancellation popup showed return date, required reason, and the manager/HR refund explanation; `/LeaveApprovals` rendered the separate `İzin İptal Talepleri` decision queue, keeping cancellation decisions out of the employee request page.
- `/LeaveBalances` expanded details rendered whole amounts without `.0`. Its edit popup exposed enabled `Hak Edilen`, `Devreden`, and `Kullanılan` controls with values `30`, `9`, and `17`, each using `step=0.5`; no write was submitted during browser validation.
- Desktop 1280x900 and mobile 390x844 checks found no document/dialog horizontal overflow or unhandled-error marker. The mobile cancellation dialog measured 326px wide at x=32 within the 390px viewport and remained readable without clipping. A final fresh browser tab completed filtered audit search with zero warning/error log entries; earlier connection entries came only from intentionally restarting the validation server while applying the new migration/build.
- The existing DEVELOPMENT database received `20260806105206_AddLeaveCancellationWorkflowAndRetrospectiveRequests`. EF reports no pending model changes. The full Release suite passed 189/189, the Release solution build passed with zero errors, and `git diff --check` passed; the existing AngleSharp `NU1902` advisory remains unchanged.

## Evidence: 2026-08-06 Leave Request Popup, Public Holidays, and Used-Day Correction
- The retained request form layout now has one popup authority; no full-page editor path remains.
- `/LeaveRequests` keeps the server-paged list rendered and opens its single create/edit `EditForm` in one `MudDialog`. At 1280x900 the dialog measured 1216x832 inside the viewport; at 390x844 it measured 326x776 with a one-column form/summary layout. Both viewports had no document-level or dialog-level horizontal overflow, and the route remained `/LeaveRequests` while the popup was open.
- `/LeaveTracking` receives named holidays from the same persisted `PublicHolidays` authority used by leave calculation. The August 2026 runtime rendered `bayram mubarak` once as a blue `Resmî tatil` calendar surface (`rgb(239, 246, 255)` with `rgb(29, 78, 216)` inset marker) and exposed the matching legend; the desktop calendar retained seven columns and mobile used the existing agenda layout without horizontal overflow.
- `/LeaveBalances` edit rendered one `Kullanılan Gün` numeric control alongside hak edilen and devreden days. All three controls expose `step=0.5`; the service rejects other decimal granularity, recalculates remaining days from the corrected used amount, records the old/new usage in audit, and later approvals add to the correction while retaining allocation provenance.
- Authenticated checks used the existing administrator session and persisted data without submitting any leave, balance, or holiday mutation. The changed routes showed no unhandled-error marker; a fresh validation tab had no browser warning/error entries. The only earlier WebSocket error came from intentionally replacing the local validation server before the clean-tab check.
- No external visual dependency or schema change was added. Existing authentication, permissions, request approval, public-holiday persistence, row-version concurrency, immutable allocation provenance, and service mutation ownership remain in force. Corrected `UsedDays` is the single current capacity authority.
- The focused changed-area suite passed 90/90, the full Release suite passed 177/177, the Release solution build completed with zero errors, and `git diff --check` passed. The existing AngleSharp `NU1902` advisory remains unchanged.

## Evidence: 2026-08-05 Stitch Kontrol Merkezi Redesign and Bounded Request History
- Stitch project `16849257981238772896`, screen `1645c12ca90a4d18a63af804bdfaf4bb` established the redesigned `/` information hierarchy while preserving the Koopbank `#C8102E` primary, `#111111` shell, `#19764A` success, warm-neutral background, white-card, Inter, 8px-radius, no-gradient visual authority.
- `/` now presents a compact welcome/action header, four summary metrics, latest-year leave balances with usage progress, an employee summary, at most three pending-request previews, and a completed-history summary. The former unbounded completed-request table and its client-side list state were removed; `Tüm Talepleri Görüntüle` routes to the existing server-paged `/LeaveRequests` authority with the selected employee context, while `Yeni Talep Oluştur` opens the popup editor with that same authorized employee preselected.
- The authenticated real MSSQL runtime proved the Dashboard balance query selects only the latest persisted year per leave type, the pending preview translates to SQL `TOP(3)`, and pending/completed/approved/rejected/latest-decision values translate to a single aggregate `COUNT/MAX` query. No completed request entities are loaded by the Dashboard.
- At 1280x720 the four metrics, two-column overview, pending preview and history action rendered inside the 960px content area with `documentScrollWidth == viewportWidth == 1280`. At 390x844 the Dashboard rendered as one 358px column with full-width actions and `documentScrollWidth == viewportWidth == 390`; the visually inspected regular screenshot showed readable hierarchy, controls and card content without clipping or overlap.
- The signed-in user's history action navigated to `/LeaveRequests?employeeId=1`, where the employee filter and existing MudTable pager rendered; its create action opened `/LeaveRequests?employeeId=1&create=true` directly in the editor. Elevated cross-employee verification repeated the flow from `/?employeeId=2`, preserved `employeeId=2`, and preselected that employee rather than the administrator or alphabetic first record. Fresh-tab browser warning/error logs were empty and no Blazor error marker appeared.
- Four executable Dashboard query/link tests cover relational latest-year selection, deterministic three-row preview ordering, employee-scoped aggregate/empty summaries, and canonical deep-link construction. The focused Dashboard/management suite passed 39/39, the full Release suite passed 173/173, the Release solution build completed with zero errors, and `git diff --check` passed. The existing AngleSharp `NU1902` advisory remains unchanged.
- The required independent re-review confirmed the selected-employee deep-link contract, fail-closed authorization, query authority, bounded data access, responsive/accessibility behavior, test coverage, documentation synchronization, and complete removal of the unbounded completed-list path; final grade: 10/10 production-grade.

## Evidence: 2026-08-04 Stitch Leave Request Form Fidelity and Shared Red Create Actions
- `/LeaveRequests` form layout was aligned more closely to Stitch screen `0d3f697b01e74398a77563a5f20a93ea` and is retained inside the current popup: the title presents the `Yeni Talep` context, the 2/3 form surface uses icon-led section dividers, employee/delegate fields share the owner grid, leave balance uses a readable light-red chip, half-day selection has its own neutral panel, and the 1/3 summary owns the date, work-day, current-balance, remaining-balance, calendar note, and Vazgeç/Gönder actions.
- Employee, leave type, normal date range, half-day date, and request reason controls expose their required state before submission with the same validation messages enforced by the existing form model.
- Existing employee/delegate eligibility, single date-range authority, half-day calculation, available-balance loading, validation, confirmation, permissions, audit, and `LeaveRequestService` mutation ownership remain unchanged. All summary values come from the active form and calculated balance state; no display-only sample data was introduced.
- `ManagementCreateButton` remains the only Add icon authority and now guarantees the same filled Koopbank-red, white-text, 44px-high, 8px-radius treatment inside and outside management tables. Repository scanning found no `MudFab` and no page-local Add icon path; `/Departments` rendered the shared `Departman Oluştur` button at 44px with `rgb(200, 16, 46)` background, white text, and 8px radius.
- The 2026-08-06 popup evidence above supersedes the earlier editor geometry while preserving the same responsive 2/3–1/3 desktop layout and single-column mobile layout.
- The dense `/Employees` toolbar was additionally checked at 390x844. Its filter, Excel group, and shared `Çalışan Oluştur` action wrapped within a 356px toolbar; the create action remained visible at 178.664x44px with red background, white text, 8px radius, zero FABs, no Blazor error marker, and `documentScrollWidth == viewportWidth == 390`.
- The focused management/personnel UI contract suite passed 44/44, the full Release suite passed 168/168, the Release solution build completed with zero errors, and `git diff --check` completed successfully. The existing AngleSharp `NU1902` advisory and lowercase migration-name warnings remain unchanged. Independent re-review found no remaining form-fidelity, validation, shared-action, responsive, accessibility, or authority gap; final grade: 10/10 production-grade.

## Evidence: 2026-08-04 Stitch Leave Request Workspace and Shared Create Action
- Stitch screens `5bd4f622bc0e49b7bcd4bca35c4e009a` and `0d3f697b01e74398a77563a5f20a93ea` are implemented as the `/LeaveRequests` list and popup create/edit workspace. The list has one header-level `Talep Oluştur` action, status tabs, result count, the shared filter action, and the existing server-backed table; the obsolete full-page editor state and page-local FAB path are removed completely.
- Create and edit now share one `IsLeaveRequestEditorOpen` state and one `EditForm`. Dynamic employee, leave option, date, requested-day, current-balance, and projected-balance values populate the summary; the existing validation, confirmation, permission checks, service calls, auditing, and reload flow remain unchanged. Status tabs write to the existing `SearchForm.Status` authority and reload the same table query instead of introducing parallel filtering.
- `ManagementCreateButton` is the single filled-primary add/create action format. All management and personnel pages with create/add actions use the component; the termination edit-only action retains its separate edit semantics.
- Current popup geometry, role, and responsive evidence is recorded in the 2026-08-06 section above. The `Reddedildi` status tab still writes the active server-side filter and the shared list remains the single table authority behind the popup.
- The focused management/personnel UI contract suite passed 44/44, the full Release suite passed 168/168, the Release solution build completed with zero errors and the two pre-existing lowercase migration-name warnings, and `git diff --check` completed successfully. The existing AngleSharp `NU1902` advisory remains unchanged. Independent post-implementation review verified explicit `aria-pressed` values, the absence of page-local create/Add paths, responsive behavior, and preserved service ownership; final grade: 10/10 production-grade.

## Evidence: 2026-08-04 Pending Approval Single Action
- `/LeaveApprovals` pending-request rows now expose only the existing `Karar Ver` action; the adjacent delete control and call site were removed from `Gelen Talepler`. Completed records in `Karar Geçmişi` retain their existing delete action.
- The authenticated route rendered at 1280x720 and 390x844 without document-level horizontal overflow. The active database had zero pending approvals, so no record was manufactured solely for visual evidence; the source contract proves one `Karar Ver` control and only one delete template, owned by the history table. Five existing history delete actions rendered normally.
- Decision dialog behavior, approval/rejection workflow, deletion service ownership for history records, filters, paging, permissions, and auditing remain unchanged. No external visual dependency was added.
- The focused management UI contract suite passed 27/27, the full Release suite passed 166/166, the Release application build completed with zero errors and only the two pre-existing lowercase migration-name warnings, and `git diff --check` completed successfully.

## Evidence: 2026-08-04 Expandable Employee Leave Balances
- `/LeaveBalances` now pages and presents employees as the primary rows. Each row shows the employee identity, department, matching balance count, and canonical current total remaining days; selecting the employee name reveals the underlying leave types, year, entitled, carry-over, used, remaining, edit, and delete controls.
- Expansion state is owned by a set of employee identifiers, so opening one employee does not close another. The browser interaction opened two employees simultaneously and rendered their independent three- and four-record detail regions; the third employee remained collapsed with correct `aria-expanded` state.
- Server paging now counts distinct matching employees while loading only the filtered balances for employees on the current page. Missing groups caused by a concurrent deletion are omitted and reconciled instead of being dereferenced. Summary totals reuse `LeaveBalanceDashboardSummary`, so historical years stay visible in details without being double-counted as currently usable leave. Existing employee, department, leave type, and year filters; assignment scopes; service-owned create/update/delete operations; confirmations; permissions; and auditing remain unchanged.
- At 1280x720, three employee summaries and two expanded detail regions rendered without document-level horizontal overflow. At 390x844, both expanded regions occupied the full 356px table content width, retained readable two-column detail cards and accessible row actions, and had no document-level horizontal overflow. The detail grid uses native table headers and row headers plus explicit mobile cell labels.
- No external visual dependency was added. The focused management UI contract suite passed 27/27, the full Release suite passed 166/166, the Release application build completed with zero errors and only the two pre-existing lowercase migration-name warnings, and `git diff --check` completed successfully.

## Evidence: 2026-08-04 Shared Management Filter and Delete Actions
- The outlined primary `Filtrele` action from `/LeaveApprovals` is now the single shared `ManagementFilterButton` authority on all seven searchable management pages: `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, `/LeaveApprovals`, and `/LeaveCarryOverWarnings`. The former page-local search-icon buttons were removed completely.
- Both `/LeaveApprovals` delete actions now use the same small outlined error icon-button format as the other management pages. Delete behavior, confirmation, permissions, audit ownership, and service calls are unchanged.
- At 1280x720, `/LeaveApprovals` and `/Departments` rendered identical 112.44x32 filter buttons with the same outlined-primary classes. The two visible approval-history delete buttons and the department delete button all rendered as 40x40 outlined-error controls; neither route had document-level horizontal overflow.
- At 390x844, the filter action remained visible at 112.44x32 on both `/LeaveApprovals` and `/Employees`, and neither route had document-level horizontal overflow. No external visual dependency was added.
- The focused management UI contract suite passed 26/26, the full Release suite passed 165/165, the Release application build completed with zero errors and the two pre-existing lowercase migration-name warnings, and `git diff --check` completed successfully.

## Evidence: 2026-08-04 Stitch Leave Approval Operations Queue
- Stitch screens `7e12ab5755d74b5fa559ec980166b629` and `87c078ce9ad54eb5a0d3266e4aab2c55` are implemented together as the `/LeaveApprovals` UI authority: pending records render in `Gelen Talepler`, completed records render in `Karar Geçmişi`, and each pending row exposes one explicit `Karar Ver` action rather than parallel row-level approve/reject controls.
- The decision dialog repeats the request ID, requester, leave type, requested days, approver role, and approver before offering equal Onayla/Reddet choices. The submit action changes label, icon, and semantic color with the selected decision; rejection retains the existing required-reason validation and all writes remain owned by `LeaveRequestService`.
- Both server-backed tables use independent factory-created read contexts, preserving the existing visibility query authority while avoiding shared-circuit DbContext concurrency. Existing search, paging, deletion, delegation transfer, authentication, permissions, audit, and workflow behavior remain in place.
- At 1280x720 the authenticated route rendered the two white operation surfaces, readable history rows and semantic status chips with no document overflow, Blazor error marker, or browser warning/error. The active database had zero visible pending approvals, so no production-like record was manufactured solely to open the decision dialog; that state and complete single-decision flow are covered by the focused UI contract test.
- At 390x844 both sections rendered at 358px width with readable responsive table rows and no document-level horizontal overflow, Blazor error marker, or browser warning/error. No external visual dependency was added.
- The full Release suite passed 164/164, the Release application build completed with zero errors and the two pre-existing lowercase migration-name warnings, and `git diff --check` completed successfully.

## Evidence: 2026-08-04 Koopbank Shell Assets
- The authenticated shell rendered the supplied Koopbank logo as the single 38x38 navbar brand mark at 1280x720 and in the open mobile drawer at 390x844. Both viewports had no document-level horizontal overflow, Blazor error marker, overlap, or unreadable navigation state.
- The supplied favicon is the single active favicon authority. Its fingerprinted `.ico` URL returned HTTP 200 with `image/x-icon`; the fingerprinted navbar logo returned HTTP 200 with `image/jpeg`, and the retired `favicon.png` reference and asset were removed.
- Browser warning/error logs were empty. Existing shell text, navigation, active state, authentication, permissions, dashboard data, and workflow ownership remained unchanged; no external visual dependency was added.
- The focused Koopbank shell contract test and the full Release suite passed (163/163), and `git diff --check` completed successfully.

## Evidence: 2026-08-03 Balance-Backed Leave Requests and Mobilization
- The authenticated real-browser flow opened `/LeaveRequests`, selected `E2E Yönetici`, and observed the manually created `E2E Manuel İzin (30 gün)` option only because that employee had a positive current-year balance. The option was selected through the real MudBlazor control.
- The same male employee received `Seferberlik İzni (2 gün)` from startup automatic reconciliation. Focused backend tests separately prove female employees are not assigned the balance and cannot request it even if an invalid balance is injected.
- `/LeaveRequests` and the related management routes rendered without document-level horizontal overflow at 1280x900, 834x1112, 390x844, 390x650, and 844x390. The Turkish date-range picker rendered a Turkish month, and browser console, page-error, and HTTP 5xx collections were empty.
- Existing management-shell typography, spacing, dialog, select, table, feedback, navigation, authentication, permission, and service-owned workflow patterns were retained; no external visual dependency was added.
- The full Release suite passed 152/152 tests, the Release solution build passed, EF reported no pending model changes, and the disposable MSSQL/browser scenario passed including previous-model DEVELOPMENT cutover and restart idempotency.

## Required Checks
- Desktop viewport renders without blank content, unreadable text, incoherent overlap, or broken navigation states.
- Mobile viewport renders without blank content, unreadable text, incoherent overlap, or horizontal layout failure.
- Authenticated shell navigation shows only permitted links and has a clear active state.
- Login, authenticated dashboard, and changed routes render without unhandled exception markers.
- Tables, cards, buttons, inputs, labels, and chips keep stable spacing and readable contrast.
- External visual dependencies are avoided unless explicitly approved and documented.
- Existing behavior, data authority, authentication, permissions, and workflow ownership remain unchanged.

## Evidence: 2026-07-30 Navigation Hierarchy, Edit Actions, and Combined Personnel Information
- At 1280x900 the `Personel Bilgileri` navigation group expanded into a distinct recessed child surface with a one-pixel hierarchy rail, connector branches, 40-pixel child rows, reduced child icons, and eight authorized admin links: seven ordinary personnel sections plus the permission-gated termination section. There was no document-level horizontal overflow or unhandled-exception marker.
- At 390x844 the responsive drawer retained the same child hierarchy, hid horizontal overflow, and remained vertically scrollable. Opening the drawer and personnel group produced no document-level horizontal overflow or browser warning/error.
- Row edit actions render as blue `#1D4ED8` controls on `#EFF6FF` with `#BFDBFE` borders, while destructive delete actions remain red. The combined personal-details edit button uses the same blue information role; add and primary workflow actions remain Koopbank red.
- `/EmployeePersonnelInformation` is now the single sicil, personal, profile-photo, and organization surface. The admin render contained eight authorized tabs, one profile-photo file input, and the profile and organization cards; at 390x844 its tab grid formed two 166-pixel columns and both merged cards rendered at the full 358-pixel content width without document overflow.
- The former separate general-information route, navigation link, tab, component, active E2E route, and canonical documentation references were removed. Requesting the retired route rendered the application's normal `Bulunamadı` state rather than a parallel compatibility surface.
- No external dependency was added. Authentication, employee scoping, profile-photo service authority, permissions, audit behavior, and CRUD workflow ownership are unchanged.
- `dotnet test IKSolution.slnx -c Release --no-restore` passed 124/124 tests, `dotnet build IK.Web.csproj -c Release --no-restore` completed with zero warnings and errors, the E2E JavaScript syntax check passed, and `git diff --check` was clean.

## Evidence: 2026-07-30 Control Center Hero Readability
- The authenticated dashboard hero rendered at 1280x900 as a 960x254 white surface with dark `#171717` heading text, muted `#66615F` supporting text, a four-pixel Koopbank-red orientation rail, and a reduced 116-pixel avatar. The former full black surface and oversized decorative red circle are absent.
- Measured hero contrast ratios are 17.93:1 for the heading, 6.10:1 for supporting copy, and 7.82:1 for the Koopbank overline against the white panel.
- At 390x844 the hero rendered as a single-column 358-pixel-wide panel with an 88-pixel avatar, 32-pixel heading, and two chips using 12-pixel text with a 1.25 line height; no unhandled-exception marker or document-level horizontal overflow appeared.
- Browser logs contained only the expected Blazor connection information. The hero remains within the existing dashboard markup and data flow; authentication, permissions, queries, and workflow ownership are unchanged.
- No external visual dependency was added. The focused Koopbank theme contract test, Release build, full Release test suite, and `git diff --check` completed successfully.

## Evidence: 2026-07-30 KKTC Koopbank Stitch Theme
- Stitch project `projects/14648098261382298391` and design system `assets/12868414108831511844` were created successfully for the red `#C8102E`, black `#111111`, white, and warm-neutral visual direction. Stitch screen generation returned a transport error and was not retried in accordance with the connector instruction; no generated screen was claimed.
- The authenticated dashboard initially rendered at 1280x900 with one dashboard shell, four summary cards, black hero surface, red hero rail, red active `Kontrol Merkezi` navigation, white active text, no unhandled-exception marker, and no document-level horizontal overflow. This initial black hero evidence is historical and was superseded by the `Control Center Hero Readability` pass above.
- The dashboard rendered at 390x844 with a hidden responsive drawer, readable app bar, avatar, hero copy, four single-column metric cards, employee detail, leave balance, pending/completed empty states, no unhandled-exception marker, and no document-level horizontal overflow. The initial viewport and lower content were visually inspected separately because the browser's full-page mobile capture composited incorrectly even though DOM geometry and normal viewport captures were correct.
- The login route rendered at 1280x900 with the KOOPBANK header, red card rail, red submit action, one login card, no unhandled-exception marker, and no horizontal overflow.
- Authenticated `/Employees`, `/LeaveRequests`, `/LeaveTracking`, and `/AuditLogs` checks rendered the expected management shells and red active-navigation state without an unhandled-exception marker or document-level horizontal overflow. A fresh smoke tab reported no browser warning or error log. The post-review `/LeaveTracking` rerun additionally verified the current-day red `#C8102E`, warm outline `#DEDAD7`, token-backed neutral/semantic surfaces, and complete removal of the route's superseded slate/indigo constants.
- At 390x844 the shell exposed the uniquely named `Menüyü aç/kapat` button; activating it opened the black responsive drawer with the red `İzin Takip` active state, no unhandled-exception marker, and no document-level horizontal overflow.
- The final contrast audit measured white active-tab text on `#A50D26` at 7.82:1, warning text `#754600` on `#FFF4DF` at 7.32:1, success text `#19764A` on `#E7F6EE` at 5.04:1, primary white-on-red at 5.88:1, and white-on-black at 18.88:1.
- No external visual dependency was added. The dashboard data queries, latest-year-per-leave-type balance authority, authentication, permissions, page-access decisions, workflow services, and audit ownership are unchanged.
- `dotnet test IKSolution.slnx -c Release --no-restore` passed 123/123 tests, the focused Release application build completed with zero errors, `git diff --check` passed, and every implementation file remained confined to the active `codex/theme` branch.

## Evidence: 2026-07-13 Stitch Dashboard Application
- Desktop dashboard checked at 1280x900 with `admin/admin123`; `/` rendered the Stitch dashboard without blank content, overlap, or unreadable active navigation.
- Mobile dashboard checked at 390x844 with `admin/admin123`; cards and tables collapsed into readable single-column responsive layouts without horizontal failure.
- Login checked at 1280x900; app bar, card, inputs, and submit button rendered without overlap.
- Navigation active state checked by computed style: `Dashboard` rendered as white text on the indigo active background with a secondary left border.
- Authenticated route smoke checked `/Employees`, `/Departments`, `/LeaveRequests`, `/LeaveApprovals`, and `/AuditLogs`; no unhandled exception marker appeared.
- Permission-gated shell checked with `admin/admin123`; 8 available modules appeared, matching the admin permission set.
- `user/user123` and `hr/hr123` returned `/login?error=unconfigured` because the active local database does not contain their linked employee rows; no seed data was mutated for this UI task.
- The external Google Fonts dependency was removed; typography now uses the local/system font stack.

## Evidence: 2026-07-13 Leave Balance Name Display
- Leave balance table checked so `Çalışan` and `İzin Türü` columns render employee and leave type names instead of raw `EmployeeId` and `LeaveTypeId` values.
- Leave balance search dialog checked so employee and leave type filters use typed name text fields, matching the other search dialogs instead of exposing numeric ID fields or fixed selects.
- Existing create/edit form behavior remains service-backed and continues to submit the selected IDs internally without exposing the IDs as table/search labels.

## Evidence: 2026-07-13 Leave Request Name Display
- Leave request table checked so `Çalışan` and `İzin Türü` columns render employee and leave type names instead of raw `EmployeeId` and `LeaveTypeId` values.
- Leave request search dialog checked so employee and leave type filters use typed name text fields instead of numeric ID fields.
- Existing create/edit form behavior remains service-backed and continues to submit the selected IDs internally.

## Evidence: 2026-07-14 Stitch Management Pattern
- Stitch project `projects/12904864656943999098` was fetched successfully with OWNER access; screen `İzin Talepleri Paneli` supplied the Professional HR Authority tokens and layout reference.
- `dotnet build IK.Web.csproj` passed with only the existing `initial` migration type-name warnings.
- `dotnet test Tests/IK.Web.Tests.csproj` passed 2 tests.
- Authenticated Playwright smoke with `admin/admin123` checked `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, `/LeaveApprovals`, and `/AuditLogs` at 1280x900; every route rendered one `management-shell`, one `management-data-table`, no exception marker, no body-level horizontal overflow, and a clear active navigation state.
- Desktop management screenshot evidence was captured for `/LeaveRequests` and `/LeaveApprovals`; icon actions, table headers, cards, and active navigation rendered without clipping or incoherent overlap.
- Mobile `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, `/LeaveApprovals`, and `/AuditLogs` were checked at 390x844; every route rendered one management shell, one management table, readable content, no exception marker, no body-level horizontal layout failure, and accessible labels on all toolbar and row action icons.
- No external visual dependency was added; the design continues to use the local/system typography stack and existing MudBlazor assets.
- Existing data authority, authentication, permissions, leave workflow services, and audit ownership were not changed.

## Evidence: 2026-07-14 Dashboard Leave Balance Total
- Dashboard total remaining-days logic checked so the `Toplam Kalan Gün` card counts only the latest yearly balance per leave type while still summing different leave types.
- `dotnet build IK.Web.csproj` passed with only the existing `initial` migration type-name warnings.
- `dotnet test Tests/IK.Web.Tests.csproj` passed 4 tests, including focused coverage for same leave type across years plus a different leave type.
- Authenticated Playwright smoke with `admin/admin123` checked `/` at 1280x900 and 390x844; each viewport rendered one `dashboard-shell`, the `Toplam Kalan Gün` card displayed the active leave-type caption, no exception marker appeared, and there was no document-level horizontal overflow.
- Existing leave renewal, carry-over, request approval, authentication, permission, and audit authorities were not changed.

## Evidence: 2026-07-14 Employee Department Manager Names
- Employee table checked so `Departman` and `Yönetici` columns render relationship names instead of raw `DepartmentId` and `ManagerId` values.
- Employee search dialog checked so department and manager filters use typed name text fields instead of numeric ID fields.
- `dotnet test IKSolution.slnx` passed 4 tests; only existing migration and unrelated leave page nullable warnings appeared.
- Authenticated Playwright smoke with `hr/hr123` checked `/Employees` at 1280x900 and 390x844; each viewport rendered one `management-shell`, one `management-data-table`, active `Çalışanlar` navigation, no exception marker, no document-level horizontal overflow, and no `Departman ID` or `Yönetici ID` labels in the table/search UI.
- Existing employee create/edit persistence remains FK-backed internally and no authentication, permission, audit, or workflow authority was changed.

## Evidence: 2026-07-14 Leave Approval Leave Type
- Leave approval table checked so `İzin Türü` renders the related leave request's leave type name.
- Leave approval search dialog checked so leave approvals can be filtered by typed leave type name.
- `dotnet test IKSolution.slnx` passed 4 tests; only existing migration and unrelated leave page nullable warnings appeared.
- Authenticated Playwright smoke with `hr/hr123` checked `/LeaveApprovals` at 1280x900 and 390x844; each viewport rendered one `management-shell`, one `management-data-table`, active `İzin Onayları` navigation, no exception marker, no document-level horizontal overflow, and `İzin Türü` in both the table labels and search dialog.
- Existing approval decision behavior remains service-backed through `LeaveRequestService`; no approval workflow, authentication, permission, audit, or balance authority was changed.

## Evidence: 2026-07-14 Audit Log Pagination
- Audit log table checked so `/AuditLogs` uses server-side pagination instead of the previous fixed latest-row limit; the page header now states that all audit rows are viewed page by page.
- `dotnet build IKSolution.slnx` passed.
- `dotnet test IKSolution.slnx` passed 5 tests, including focused audit log pagination coverage for total count, stable ordering, second page, and rows beyond the previous 50-row limit.
- Authenticated Playwright smoke with `admin/admin123` checked `/AuditLogs` at 1280x900 and 390x844; each viewport rendered one `management-shell`, one `management-data-table`, the pager label `Sayfa başına kayıt:`, no exception marker, and no document-level horizontal overflow.
- Desktop pager navigation moved from `1-25 / 355` to `26-50 / 355`, and the first visible audit row changed from `355` to `330`, confirming older audit rows are reachable through pagination.
- Existing audit creation authority remains service-backed through `AuditLogService`; authentication, permissions, workflow ownership, and audit write behavior were not changed.

## Evidence: 2026-07-15 Management Pagination Rollout
- `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, and `/LeaveApprovals` now use the same MudBlazor server-side table pagination pattern as `/AuditLogs`; fixed `Recent*` table lists and `Take(25)` display limits were removed from those management pages.
- `dotnet build IKSolution.slnx` passed with only the existing lowercase migration-name warnings.
- `dotnet test IKSolution.slnx` passed 8 tests, including focused shared management pagination coverage for unpaged total count, second-page access, filtered count before paging, and invalid page input normalization.
- Authenticated Playwright smoke with `admin/admin123` checked `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, `/LeaveApprovals`, and `/AuditLogs` at 1280x900 and 390x844; every route rendered one `management-shell`, one `management-data-table`, the pager label `Sayfa başına kayıt:`, no exception marker, and no document-level horizontal overflow.
- Server log polling after the final Playwright smoke showed expected EF SQL output only; no DbContext concurrency exception or Blazor unhandled exception was logged.
- Existing search filters, permissions, authentication, audit writes, and leave workflow service ownership were not changed; searches now reload the first server-side page instead of replacing the table with a fixed latest 25-row list.

## Evidence: 2026-07-15 Management Filter UX and Table Scroll
- `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, and `/LeaveApprovals` search dialogs were checked with 6, 2, 1, 2, 2, and 3 autocomplete controls respectively; numeric, date, and enum filters retained their appropriate specialized controls.
- Applied-filter state was checked on `/LeaveRequests`: the table toolbar displayed `Talep ID: 3` on the left, its accessible remove control restored the unfiltered table, and both the toolbar-wide `Tümünü temizle` action and the search-dialog `Filtreleri Temizle` action removed all applied filters.
- Search-dialog draft isolation was checked through the implementation and browser flow: `İptal` discards draft edits, while `Ara` applies the draft and reloads page zero.
- The shared management table viewport was checked with one filtered row and 25 audit rows: one row had no maximum-height constraint, while the 25-row audit page used a 1008px internal viewport with `overflow-y: auto`, `scrollHeight 1648 > clientHeight 1008`, and a sticky table header. This confirms scrolling begins only when a 16th rendered row exists.
- `dotnet build IK.Web.csproj -c Release --no-restore` passed with only the two existing lowercase migration-name warnings.
- `dotnet test Tests/IK.Web.Tests.csproj -c Release --no-restore` passed 26 tests, including focused autocomplete option matching, text normalization, active-filter callbacks, approval visibility scoping, SQL-side distinct option projection, and complete six-page/seven-table UI contract coverage.
- Authenticated Playwright smoke with `admin/admin123` checked `/Employees`, `/Departments`, `/LeaveTypes`, `/LeaveBalances`, `/LeaveRequests`, `/LeaveApprovals`, and `/AuditLogs` at 1280x900 and 390x844; every route rendered one management shell and management table with zero document-level horizontal overflow, no browser page errors, and no unhandled exception marker.
- Desktop and mobile visual inspection confirmed readable autocomplete suggestions, filter chips, individual close controls, wrapped mobile toolbar actions, table content, pager controls, and stable contrast without overlap or clipping.
- No external runtime visual dependency was added. Department parent filtering intentionally moved from raw parent ID equality to autocomplete parent-name matching; page-specific visibility and permission rules, authentication, audit writes, and leave workflow service authority remain unchanged.

## Evidence: 2026-07-16 Working-Day and Half-Day Leave Requests
- `/LeaveRequests` create/edit replaced time-derived duration with date-only selection plus an explicit `Yarım gün izin` switch; the form displays the authoritative requested working-day amount immediately and states that weekends are excluded.
- Submit confirmation was exercised with a half-day request: the dialog stated that `0,5` working day would be deducted after final approval, and the validation flow cancelled at confirmation so no UI-smoke request was persisted.
- `dotnet test IKSolution.slnx -c Release --no-restore` passed 49 tests, including weekend exclusion, weekend-only rejection, weekend-only overlap allowance, weekday-overlap rejection, explicit half-day create/update, `0.5` final balance deduction, weekend-spanning final balance deduction, pre-cutover pending-request recalculation, date-only MSSQL model mapping, and the leave-request UI contract.
- The date-only migration script was generated from `20260709062323_initial3` to `20260716112454_UseDateOnlyLeaveRequestPeriod`; it drops the dependent period index/check constraint, alters both request period columns to MSSQL `date`, and recreates the index/check constraint in dependency-safe order.
- Authenticated Playwright smoke with `admin/admin123` checked `/LeaveRequests` at 1280x900 and 390x844; both viewports rendered the management shell, active leave-request navigation, date inputs, half-day switch, working-day alert, table, and actions without browser page errors, unhandled exception markers, document-level horizontal overflow, clipping, or unreadable contrast.
- The changed dialog exposed no native time inputs. Desktop and mobile screenshots were visually inspected; fields, labels, switch, alert, and actions retained stable spacing and remained readable.
- No external visual dependency was added. Existing authentication, permissions, manager/HR workflow ownership, audit ownership, and final-approval balance authority remain service-backed.
- The required independent post-implementation review reran Release tests/build, migration/model checks, and diff validation and graded the final implementation 10/10.

## Evidence: 2026-07-16 Single Leave Period Picker
- `/LeaveRequests` create/edit now uses one `MudDateRangePicker` for full-day periods instead of separate start/end date controls; the form keeps one `DateRange` authority for submission, preview, create, and edit behavior.
- Browser interaction selected 17–20 July 2026 from the range picker and displayed `2` working days. Enabling half-day immediately selected the range start, 17 July, displayed `0,5`, and the same field position then accepted 21 July as a different half-day date while retaining `0,5`.
- `dotnet test IKSolution.slnx -c Release --no-restore` passed 50 tests, including the focused single-range-authority and half-day-default UI contract.
- Authenticated Playwright smoke with `admin/admin123` checked the changed dialog at 1280x900 and 390x844; the range fields, conditional half-day date field, switch, working-day alert, and actions remained readable without browser page errors or document-level horizontal overflow.
- Desktop and mobile screenshots were visually inspected. The full-day control remained a single responsive selection area, labels and contrast were readable, and no external visual dependency was added.
- The required independent post-implementation review reran all 50 Release tests, the solution Release build, and diff validation and graded the final single-picker implementation 10/10.

## Evidence: 2026-07-16 Projected Remaining Leave Preview
- `/LeaveRequests` preloads each authorized employee's total remaining days with the same `LeaveBalanceDashboardSummary.SumCurrentRemainingDays` authority used by the Home dashboard, then subtracts the live working-day preview without mutating any balance or starting a database query during employee selection.
- Authenticated Playwright interaction selected the Home employee in the request form and verified exact arithmetic against Home's `Toplam Kalan Gün`: a two-workday range showed the total minus `2`, then half-day mode showed the total minus `0,5`.
- The projected text explicitly states that the balance applies when the request is approved, matching the service-owned final-approval deduction lifecycle.
- `dotnet test IKSolution.slnx -c Release --no-restore` passed all 53 tests, including exact full-day and half-day projected-balance arithmetic plus the updated leave-request UI contract.
- The changed dialog was checked at 1280x900 and 390x844 with no browser page errors or document-level horizontal overflow. Desktop and mobile screenshots were visually inspected; the longer alert wrapped cleanly and remained readable without overlap or clipping.
- The required independent post-implementation review verified authorized-data scoping, race-free synchronous employee selection, exact arithmetic, lifecycle resets, tests, docs, and diff cleanliness and graded the final implementation 10/10 production-grade.

## Evidence: 2026-07-27 Personnel Tabs and Record-Linked Files
- The eight ordinary-employee tabs and admin-only termination tab use one `PersonnelInformationTabs` component. All personnel CRUD pages now reuse the established management shell, `MudDialog` forms, table toolbar `MudFab`, tooltip-wrapped outlined row actions, shared table class, 8px surfaces, and existing theme tokens; the previous page-inline `MudPaper` forms were removed.
- Normal employees retain own-record access to personnel, bank, general, identity/document, phone, address, education, and course/certificate pages. `CanManageEmployeeTerminations` requires `CanCreateNewEmployee`; the navbar, shared tabs, route initialization, loads, saves, and deletes all apply that backend rule, so termination data is not queried or rendered for ordinary employees.
- The dashboard contains no file input. Profile-photo upload initially moved to a separate general-information route; the current combined personnel surface documented above supersedes that historical route. Identity, diploma, and certificate uploads are opened from their related table row and the service verifies both target-employee authorization and related-record ownership.
- Migrations `20260727110418_LinkEmployeeDocumentsToPersonnelRecords` and `20260727111934_EnforceEmployeeDocumentRelatedOwnership` were applied to the local development database. They preserve every existing document, backfill only unambiguous one-record identity/education/course matches, fail closed on any cross-employee legacy link, replace record-only FKs with restrictive `(EmployeeId, related record id)` composite FKs, and leave ambiguous legacy files available as read-only records. EF reports no pending model changes.
- The guarded `IK_E2E_OwnershipConstraint` MSSQL fixture attempted a direct cross-employee `EmployeeDocuments` insert and observed SQL Server error `547`; teardown removed the disposable database. This proves the ownership rule is enforced below the service layer.
- At that revision, bank, institution/program, and issuer names used open-ended `ManagementFilterOptionSearch` autocomplete; the 2026-07-30 evidence below supersedes that UI choice with direct text entry. Controlled document, education, phone, address, geographic, and termination categories continue to use the single select-only `PersonnelSelectOptions` authority.
- At that revision, `dotnet test Tests/IK.Web.Tests.csproj -c Release --no-restore` passed all 81 tests, including the then-current scoped manual-autocomplete contract. The 2026-07-30 evidence below records the replacement contract and current validation.
- The final `dotnet build IK.Web.csproj -c Release --no-restore` passed with zero errors; it reports only the two existing lowercase legacy migration-name warnings. `git diff --check`, the E2E JavaScript syntax check, and authenticated file-route HTTP protection checks passed; unauthenticated profile-photo and document requests returned `401`.
- The in-app Browser runtime was unavailable (`agent.browsers.list()` returned an empty collection), so desktop/mobile rendering and screenshot inspection could not be executed or claimed. The updated isolated-database browser workflow remains available at `.bet-task/scripts/run-employee-information-e2e.sh` and now covers the dashboard file-control removal, General Information profile upload, record-linked identity upload, ordinary-user termination denial, admin routes, CRUD, validation, and responsive overflow.
- The required independent post-implementation review verified the composite ownership constraints, fail-closed migration, clean-install SQL, real MSSQL rejection probe, authorization boundaries, docs, and 81-test result, with no P0/P1/P2 finding; final grade: 10/10 production-grade.

## Evidence: 2026-07-27 Personnel Selects and Address Hierarchy
- Education records now display and edit a required `Eğitim Seviyesi` select separately from the optional `Diploma/Derece Adı`; the empty configuration state disables save and explains that the system administrator has not defined options yet.
- Document type, phone type, address type, and termination reason use select-only controls. They do not accept or learn free text, and their empty configuration states keep the related save action disabled.
- The address dialog was checked in the confirmed order: address type, country, city, district/region, postal code, open address, and primary-address choice. City remains disabled until a country is selected, district remains disabled until a city is selected, and changing a parent clears its child values.
- The in-app Browser checked the changed address and education dialogs at 1280x900 and 390x844. Desktop and mobile screenshots were visually inspected; labels, helper text, controls, and actions remained readable without clipping, overlap, or document/dialog-level horizontal overflow.
- A fresh authenticated in-app Browser tab reopened the address dialog and reported zero console warnings or errors. The address-type and country controls each resolved to one disabled combobox while the option authority was intentionally empty.
- No external visual dependency was added. Existing own-record/admin authorization, audit ownership, document-record ownership, and service-backed CRUD boundaries remain unchanged.

## Evidence: 2026-07-28 Education Entry Fix
- `EducationLevels` is available in normal application builds with the user-confirmed `Ön Lisans`, `Lisans`, `Yüksek Lisans`, and `Doktora` values; the education-level control and save action are no longer disabled by an always-empty normal-build list.
- The authenticated in-app Browser opened `/EmployeeEducations`, confirmed the education-level combobox and save action were enabled, and observed all four options.
- The browser selected `Lisans`, entered `E2E Eğitim Üniversitesi`, saved successfully, and observed the persisted table row with the selected level. Browser console and warning logs were empty.
- The interaction used a disposable MSSQL database and ordinary employee ownership scope; teardown removed the database and container after verification.
- The change adds no layout markup or visual dependency. The existing desktop/mobile dialog layout evidence remains applicable; normal employee authorization, audit writes, relational persistence, and document ownership are unchanged.
- The focused education UI contract tests passed 2/2 and the Release application build passed with zero warnings or errors. The full suite reached 82/84; its two failures are pre-existing, unrelated E2E-script-text and date-sensitive leave-test regressions.
- The blank live education page was traced to MSSQL error 207: the already-applied personnel migration had been edited after execution, so the local database lacked `EmployeeEducations.EducationLevel`. `20260728093000_AddEmployeeEducationLevel` now owns that incremental schema change while the original table-creation migration is restored.
- The new migration was applied to the local development database, the application was restarted, and the authenticated in-app Browser confirmed the complete employee selector, education toolbar, add action, table headers, and empty-record state render with zero browser errors or warnings.

## Evidence: 2026-07-28 Employee Gender and Blood Group
- `/Employees` now displays nullable `Cinsiyet` and `Kan Grubu` columns and exposes both values as controlled selects in create/edit and search dialogs. Existing rows render `-` until these optional values are assigned.
- The authenticated in-app Browser confirmed the create dialog offers only `Kadın` and `Erkek`, and all eight ABO/Rh blood-group choices. The search dialog exposes the same two controlled filters; no free-text entry path exists.
- Browser checks at 1280x900 and 390x844 each rendered one management shell and one management table, with no unhandled-exception marker or document-level horizontal overflow. Browser console errors were empty.
- Focused management UI and schema contract tests passed 15/15. EF reported no pending model changes, the additive migration was applied to the local development database, and the full migration chain succeeded against a disposable MSSQL database that was removed after verification.
- The required independent post-implementation review found no actionable model, migration, CRUD, filtering, authorization, privacy, documentation, or regression gap and graded the final implementation 10/10 production-grade.

## Evidence: 2026-07-28 Employee Staff Date
- `/Employees` now keeps `Kadro Tarihi` separate from `İşe Başlama Tarihi` in the table, create/edit dialog, search dialog, active-filter chips, and server-side query. Existing rows remain valid and display `-` until an optional staff date is assigned.
- The authenticated in-app Browser confirmed the independent `Kadro Tarihi` date picker in both create and search dialogs and the separate table column.
- Stable checks at 1280x900 and 390x844 each rendered one management shell and one management table, with no unhandled-exception marker, browser console error, or document-level horizontal overflow.
- Focused management UI and schema contract tests passed 16/16. EF reported no pending model changes, the additive migration was applied to the local development database, and the full migration chain succeeded against a disposable MSSQL database that was removed after verification.
- The required independent post-implementation review found no actionable schema, migration, CRUD, filtering, UX, documentation, or regression gap and graded the final implementation 10/10 production-grade.

## Evidence: 2026-07-28 Configurable Public Holidays
- `/PublicHolidays` reuses the management shell, shared scrollable table, compact toolbar, year navigation, accessible row actions, confirmation dialog, validation feedback, success feedback, and empty state. The first screen makes the annual calendar directly actionable without introducing a new visual system.
- An ordinary authenticated employee was routed to `/unauthorized` and did not see the `Resmî Tatiller` navigation link. After administrator login, the route and navigation link were visible; backend `PageAccessService` checks also guard route initialization and every save/delete mutation.
- The in-app Browser exercised validation, create, edit, delete confirmation, successful delete, and return to the year-specific empty state at 1280x720. The regular viewport screenshot was visually inspected: hierarchy, spacing, controls, table, pager, focus/active styling, contrast, and destructive action treatment match adjacent management pages.
- The page reported `scrollWidth == clientWidth` before and after CRUD, so there was no document-level horizontal overflow. A fresh post-restart tab produced only Blazor connection information and no browser error/warning; the earlier connection errors were caused solely by intentionally restarting the isolated validation server.
- The leave-request alert now states that weekends and configured public holidays are excluded. Focused calculator/service coverage proves weekday-holiday exclusion, holiday-only rejection, holiday-only overlap allowance, and persistence-backed request calculation.

## Evidence: 2026-07-28 Employee Self-Service Gender and Blood Group
- `/EmployeePersonnelInformation` now shows the selected authorized employee's gender and blood group and exposes a focused edit dialog with controlled enum-backed selects. The existing `CanEditPersonnelInformation` authority is rechecked when opening and saving, so a normal employee can mutate only their own record while existing managers retain their scoped employee selection.
- An authenticated disposable-database browser test used the ordinary `user` account, confirmed there was no employee selector, selected `Erkek` and `AB Rh-`, saved successfully, reloaded the route, and observed both values persisted.
- The database log from that interaction showed the audit insert and employee update in the same `SaveChanges` command. Audit details contain only `Fields=Gender,BloodGroup`; neither selected value is copied into audit details.
- The final save flow carries the dialog-opening `RowVersion`, reloads current values on a concurrency conflict, disables the form during persistence, skips no-op writes, and builds audit field names only from values that actually changed.
- A final disposable-database browser regression changed only gender and the SQL command updated only `Gender` plus `UpdatedAt`. Reopening and saving without changes showed `Herhangi bir değişiklik yapılmadı.` and emitted only the employee read query, with no employee update or audit insert.
- Stable checks at 1280x900 and 390x844 had no unhandled-exception marker, browser console error, or document-level horizontal overflow. The disposable MSSQL database was removed and the isolated test server was stopped afterward.
- The required independent re-review confirmed that the concurrency, audit-accuracy, no-op, and reentrant-submit findings were resolved with no remaining actionable gap; final grade: 10/10 production-grade.

## Evidence: 2026-07-28 Personnel Excel, Required Fields, and Department Management (Excel location superseded by R24)
- The authenticated in-app Browser checked `/Employees`, `/Departments`, and `/EmployeeEducations?employeeId=1` at the default 1280x720 viewport. Each changed route rendered its shell, table, toolbar, and dialog without blank content, unhandled-exception markers, overlap, or document-level horizontal overflow.
- At this revision, the employee table and education toolbar exposed accessible `Import Excel` and `Export Excel` actions while retaining the existing create and row actions. R24 later removed Excel controls from every `Personel Bilgileri` subpage; the main employee table retains them. The department table shows `Departman Yöneticisi`, and the edit dialog explains that only active employees in that department are eligible.
- Employee submission without a department displayed `Departman seçimi zorunludur.` instead of reaching the generic database-error path. Required employee and personnel fields now show a visible required marker before submission.
- The education page and add dialog were visually checked at 390x844. The toolbar actions wrap cleanly, table rows use readable mobile labels, the dialog has no internal or document-level horizontal overflow, and institution, program, education level, and start date are visibly required.
- A fresh post-restart browser tab reported no warning/error console entries. The only earlier connection messages were caused by intentionally restarting the local validation server after rebuilding.
- No external visual runtime dependency was added. Authentication and page permissions remain backend-checked; employee manager authority moved intentionally to the department manager service, and Excel writes use the same scoped permission checks and audit service.

## Evidence: 2026-07-28 Department Parent Name and Excel Action Polish
- The department table and edit selector now present the parent department by name; internal identifiers remain persistence-only and top-level rows use the explicit `Ana departman` label.
- `İçe Aktar` and `Dışa Aktar` use one compact, accessible Excel action group with matching dimensions, typography, focus treatment, disabled behavior, and responsive two-action layout. Existing import/export behavior and manual CRUD controls are unchanged.
- The authenticated in-app Browser checked `/Departments` and `/Employees` at 1280px and 390px widths. Both parent-name values and the Excel action hierarchy were visually inspected; the narrow layout retained equal-width actions and both routes reported `scrollWidth == clientWidth`.
- The import file control exposes the accessible name `Excel dosyası içe aktar`; browser warning/error logs were empty. The isolated real-browser E2E selected an XLSX through that shared control, observed the localized success message and persisted holiday row, downloaded the employee export, selected the next eligible weekday from the date picker, completed HR approval, and verified delegation activation and restoration without browser errors across desktop, tablet, mobile, short-mobile, and landscape viewports.
- The real browser upload exposed and closed a synchronous-read defect in the import service; workbook copying is now asynchronous and a regression test uses an async-only stream matching Blazor's browser-file contract. All 109 Release tests passed, EF reports no pending model changes, and the Release build completed with zero errors.

## Evidence: 2026-07-29 Active Delegate and Leave Tracking (R19, transfer location superseded by R22)
- At R19, `/LeaveTracking` exposed month navigation, elevated department filtering, pending/approved summaries, a seven-day desktop calendar, selected-day workforce status, and the then-current manual-transfer action. R22 removed that action; `/LeaveTracking` now owns tracking only and `/LeaveApprovals` is the sole current transfer UI.
- The authenticated in-app Browser checked the route at 1280x900. The calendar rendered seven columns, the three summary cards retained equal spacing, the active navigation state was clear, and `scrollWidth == clientWidth` proved there was no document-level horizontal overflow.
- The same route was checked at 390x844. Summary cards collapsed to one column, the calendar became an agenda-style block, leading/trailing empty days were hidden, controls and legends remained readable, and `scrollWidth == clientWidth`.
- Browser warning/error logs were empty. The page uses the existing local theme/assets and adds no external visual dependency.
- The R19 isolated MSSQL/Chromium E2E approved a manager leave, observed its green calendar state with request and approval actors, exercised the then-current manual-transfer workflow, verified report and pending-approval reassignment, restored the primary manager, and included `/LeaveTracking` in desktop, tablet, mobile, short-mobile, and landscape overflow checks. R22 supersedes only the UI location, not that lifecycle evidence.
- Backend authorization tests prove ordinary employees are restricted to their own department while elevated leave viewers can query all departments and the selected-day roster. Leave reasons are not included in the tracking DTO or UI.
- All 115 Release tests passed, the Release application build passed, EF reported no pending model changes, and the combined real-runtime E2E completed successfully.

## Evidence: 2026-07-29 Active Delegate Approval Access
- `PageAccessService.CanAccessLeaveApprovalsAsync` now grants backend route and navigation access to the employee stored in `Departments.ActiveDelegateEmployeeId`, while the existing global approval permission continues to grant elevated access.
- Approval visibility remains limited by `LeaveRequest.ManagerApproverEmployeeId`, and `LeaveRequestService.ManagerDecisionAsync` still rejects every manager other than the currently assigned approver.
- Manual `Vekâleti Devret` UI and its service call exist only on `/LeaveApprovals`; `/LeaveTracking` contains no delegation service, dialog, or transfer action. After transfer, the former delegate is redirected to `/LeaveTracking` because their approval-page authority has ended.
- The authenticated in-app Browser checked `/LeaveApprovals` at 1280x900 and 390x844. Both viewports rendered the management shell, active navigation, table, readable actions, no unhandled-exception marker, and no document-level horizontal overflow.
- The authenticated Browser also checked `/LeaveTracking` at 390x844 and confirmed the page rendered without horizontal overflow or transfer UI. The local development data was read only; its current active delegate has no configured static-login account, so conditional delegate rendering was proven by focused code-contract and authorization tests rather than claimed as a browser interaction.
- `dotnet test IKSolution.slnx -c Release --no-restore` passed all 119 tests, including the integrated transfer → route access → scoped visibility → manager approval scenario and independent-context concurrency regression. A subsequent isolated `dotnet build IK.Web.csproj -c Release --no-restore` passed with zero warnings and zero errors, and `git diff --check` passed.
- Devir sonrası `/LeaveTracking` geçişi tam sayfa yenilemesi kullanır; böylece kalıcı Blazor yerleşimindeki navbar yetkileri yeniden sorgulanır ve artık aktif vekil olmayan çalışandan `İzin Onayları` bağlantısı hemen kaldırılır. UI sözleşme testi hem bu yenilemeyi hem devretme arayüzünün yalnız onay sayfasında kalmasını doğrular.
- Manuel devir adayları, açık zincirdeki diğer çalışanları döngü güvenliği için dışlamaya devam eder; yalnızca son manuel devrin bir önceki aktif vekili geri-devir adayı olarak sunulur. İkinci vekilden birinci vekile dönüş son manuel kaydı kapatır, aktif vekili ve bağlı çalışan/onay atamalarını geri taşır ve yeni bir döngü kaydı oluşturmaz.
- Navbar ile sayfa aynı anda veri yüklerken `PageAccessService` artık ortak circuit context'ini paylaşmaz; her veritabanı tabanlı erişim kontrolü factory üzerinden ayrı kısa ömürlü context kullanır. Eşzamanlı regresyon testi üç erişim sorgusunun üç ayrı context oluşturduğunu doğrular; bu yaşam süresi düzeltmesi UI yerleşimini veya veri modelini değiştirmez.
- Düzeltilmiş Release uygulaması izole `127.0.0.1:5113` giriş noktasında mevcut yönetici oturumuyla açıldı; `/LeaveApprovals` ve `/LeaveTracking` art arda render edildi. Her iki başlık doğru yüklendi, unhandled-exception işareti ve yatay taşma görülmedi, tarayıcı warning/error günlüğü boş kaldı; sunucu günlüğünde `A second operation was started` hatası oluşmadı.

## Evidence: 2026-07-29 Personnel Information Excel UI Removal
- `EmployeePersonnelInformation`, banka, genel bilgiler, kimlik-belge, telefon, adres, eğitim, kurs-sertifika ve işten ayrılma sayfalarında `ExcelImportExportActions` bulunmadığı sözleşme testiyle doğrulandı.
- Ana `/Employees` ve `/PublicHolidays` sayfaları Excel içe/dışa aktarma kontrollerini korur; manuel personel CRUD ve ilişkili belge akışları değiştirilmedi.
- Gerçek tarayıcı E2E kapsamı dokuz personel alt sayfasında `Dışa Aktar` düğmesi ve XLSX dosya girdisi bulunmamasını, iki izinli ana sayfada ise Excel kontrollerinin bulunmasını doğrulayacak şekilde güncellendi.
- İzole `127.0.0.1:5113` tarayıcı kontrolünde değiştirilen banka, kimlik-belge, telefon, adres, eğitim ve kurs-sertifika sayfalarının her birinde export düğmesi `0`, XLSX girdisi `0`, unhandled-exception işareti `false` ve yatay taşma `false` ölçüldü. `/Employees` ve `/PublicHolidays` sayfalarının her birinde bir export düğmesi ve bir XLSX girdisi kaldığı, tarayıcı warning/error günlüğünün boş olduğu doğrulandı.
- `dotnet test IKSolution.slnx -c Release --no-restore` 120/120 geçti; Release build sıfır uyarı/sıfır hata ile tamamlandı, E2E JavaScript söz dizimi ve `git diff --check` doğrulamaları geçti.

## Evidence: 2026-07-30 Personnel Open-Ended Text Fields
- Banka adı, eğitim bölüm/programı, kimlik belgesini düzenleyen kurum ve kurs/sertifikayı düzenleyen kurum artık doğrudan `MudTextField` ile girilir. Bu dört personel alt sayfasında `MudAutocomplete`, mevcut-değer seçenek state'i, seçenek sorgusu ve `ManagementFilterOptionSearch` çağrısı kalmadı.
- Kontrollü alan otoriteleri değiştirilmedi: eğitim seviyesi ve belge türü `MudSelect`/`PersonnelSelectOptions` üzerinden çalışmaya devam eder. Kaydetme, doğrulama, çalışan kapsamı, yetkilendirme, audit ve ilişkili belge sahipliği akışları aynıdır.
- Kimlik-belge, eğitim, banka ve kurs-sertifika ekleme diyalogları 1280x900 masaüstü görünümünde açıldı. Hedef alanların textbox, kontrollü alanların combobox olarak render edildiği; başlık, etiket, zorunlu alan ve eylemlerin okunabilir ve çakışmasız kaldığı doğrulandı.
- Aynı dört diyalog 390x844 mobil görünümde kontrol edildi. Her sayfada belge `scrollWidth == clientWidth` ölçüldü; diyaloglar 32px sol/sağ boşlukla görünüm içinde kaldı, yatay taşma ve unhandled-error işareti oluşmadı.
- Masaüstü banka diyaloğu ve mobil kurs/sertifika diyaloğu ekran görüntüleri görsel olarak incelendi; alan aralıkları, kontrast, tarih kontrolleri ve eylemler kırpılmadan okunabilir kaldı. Tarayıcı warning/error günlüğü boştu ve harici görsel bağımlılık eklenmedi.
- Odaklı `PersonnelInformationUiContractTests` 14/14, tam Release test paketi 120/120 geçti.
- Zorunlu bağımsız uygulama incelemesi cutover, kapsam, test, dokümantasyon ve UI kanıtında eyleme dönük boşluk bulmadı; sonuç 10/10 üretim kalitesi.

## Evidence: 2026-07-30 Public Holiday Year Navigation Spacing
- `/PublicHolidays` önceki yıl, takvim yılı ve sonraki yıl kontrollerini tek `public-holiday-year-navigation` flex grubunda tutar; sonraki yıl düğmesi artık toolbar boşluğuna dağılmak yerine yıl alanının hemen sağındadır.
- 1280x900 tarayıcı kontrolünde grup 236px genişlikte render edildi ve üç kontrol arasındaki boşluk 8px ölçüldü. Excel ve ekleme aksiyonları sağda ayrı kaldı; yatay taşma veya unhandled-error işareti oluşmadı.
- 390x844 mobil kontrolde aynı grup tek parça kaldı, toolbar kontrollü biçimde satırlara bölündü ve belge `scrollWidth == clientWidth` ölçüldü. Masaüstü ve mobil ekran görüntüleri okunabilirlik, hizalama, kırpılma ve çakışma açısından görsel olarak incelendi.
- Tarayıcı warning/error günlüğü boştu. Odaklı yerleşim sözleşme testi geçti, tam Release test paketi 121/121 geçti, Release build sıfır uyarı/sıfır hata ile tamamlandı ve `git diff --check` geçti.
- Zorunlu bağımsız inceleme yerleşim kök nedeni, responsive davranış, test ve kanıtta eyleme dönük bulgu tespit etmedi; sonuç 10/10 üretim kalitesi.

## Evidence: 2026-07-30 Responsive Personnel Information Shortcuts
- Ortak `PersonnelInformationTabs` gezinmesi yatay kaydırmalı tek satır yerine `repeat(auto-fit, minmax(155px, 1fr))` kullanan responsive grid olarak render edilir. Uzun sekme adları kendi hücresinde satır kırar; dokuz yetkili sekmenin tamamı ekranda görünür kalır.
- 1280x900 yönetici görünümünde sekmeler 185px genişlikte beş sütun ve iki satıra dağıldı. Aktif `Banka Bilgileri` sekmesi belirgin kaldı; sekme alanında `scrollWidth == clientWidth`, sayfada yatay taşma ve unhandled-error işareti yoktu.
- 390x844 mobil görünümde sekmeler 166px genişlikte iki sütuna dağıldı. Dokuz sekme, ikonları ve tam erişilebilir adlarıyla görünür kaldı; ekran görüntüsünde kırpılma, çakışma veya okunamayan metin görülmedi.
- Temiz tarayıcı sekmesinde warning/error günlüğü boştu. Odaklı responsive-grid sözleşme testi geçti, tam Release test paketi 122/122 geçti, Release build sıfır uyarı/sıfır hata ile tamamlandı ve `git diff --check` geçti.
- Değişiklik yalnızca ortak sekme yerleşimini etkiler; aktif rota, çalışan query-string'i, termination görünürlüğü, yetkilendirme ve personel CRUD davranışları değiştirilmedi.
- Zorunlu bağımsız inceleme responsive grid, uzun etiketler, dokunma hedefleri, test ve UI kanıtında eyleme dönük bulgu tespit etmedi; sonuç 10/10 üretim kalitesi.

## Evidence: 2026-07-31 Annual Leave Automation, Bulk Assignment, and Audit Log Polish
- Yönetici oturumuyla `/AuditLogs` ve `/LeaveBalances` masaüstünde görsel olarak incelendi. Denetim tablosunda varlık adı/ID sütunları bulunmuyor; kullanıcı, işlem rozeti, detay ve Türkçe tarih alanları okunabilir aralık ve kontrastla render ediliyor.
- İzin bakiyesi tablosu çalışan ve departman adlarını gösteriyor, bakiye ID göstermiyor. Tek çalışan/departman/tüm aktif çalışanlar kapsamlı atama penceresi, çalışan ve departman autocomplete kontrolleri ve 50 günlük uyarı onayı gerçek MSSQL + Chrome akışında kullanıldı.
- Görsel kontrolde seçimsiz izin türünün ham `0` değeri gösterdiği saptandı; alan nullable seçim modeliyle düzeltildi. E2E regresyonu her yeni atama penceresinde seçimsiz alanın `0` göstermediğini doğruluyor.
- Tarih aralığı seçicisi Türkçe ay adıyla render edildi. `/AuditLogs`, `/LeaveBalances`, `/LeaveTypes` ve `/LeaveRequests` masaüstü, tablet, mobil, kısa mobil ve yatay viewportlarda belgesel yatay taşma veya unhandled-exception işareti olmadan tamamlandı.
- Anonim kullanıcıların değiştirilen yönetim rotalarından giriş sayfasına yönlendirildiği, worker başlangıç telafisinin tek toplu denetim kaydı ürettiği ve uygulama yeniden başlatıldığında mükerrer otomatik hak kaydı/audit oluşmadığı doğrulandı. Tarayıcı console, page ve HTTP 500 hata listeleri boştu.
- Harici görsel bağımlılık eklenmedi. İzin bakiyesi yazma yetkisi `LeaveBalanceService`, uygunluk ve kıstelyevm hesabı `LeaveEntitlementService`, sayfa erişimi mevcut `PageAccessService` otoritesinde kalıyor.
- Odaklı izin ve UI sözleşme testleri 28/28 geçti; uygulama build'i sıfır hatayla tamamlandı ve gerçek çalışma zamanı E2E kanıtı `.bet-task/evidence/leave-automation-audit-e2e.json` içinde `PASS` olarak kaydedildi.

## Evidence: 2026-08-03 Unified Carry-Over Review Decision
- Yönetici oturumuyla izole gerçek MSSQL veritabanında toplam 60 gün ve 50 gün uyarı sınırı oluşturan bakiye hazırlandı. `/LeaveCarryOverWarnings` bekleyen satırında yalnız `İncele` eylemi render edildi; ayrı düzenleme ve salt durum-onay yolları bulunmadı.
- `İncele` penceresi devreden gün seçimi, yeni toplam, yeni kalan ve uyarı sınırını tek karar yüzeyinde gösterdi. Devreden gün 30'dan 20'ye indirildi ve tek onay sonrasında bakiye 50 güne güncellenirken aynı kayıt `Bekleyen` durumundan `İncelendi` durumuna geçti.
- Sayfa yeniden yüklendiğinde varsayılan `Bekleyen` filtresi incelenmiş kaydı gizledi. `Tümünü temizle` kullanıldığında yıl ve durum çiplerinin ikisi de kaldırıldı ve incelenmiş kayıt yeniden gösterildi; temizleme artık başlangıç filtrelerini geri yüklemiyor.
- Masaüstü karar penceresi ve 390x844 mobil incelenmiş kayıt görünümü görsel olarak incelendi. Mobil ölçümde `scrollWidth == clientWidth == 390`; kırpılma, çakışma veya yatay taşma görülmedi.
- Bilinçli sunucu yeniden başlatmasının eski Blazor bağlantı kayıtları ayrıştırıldı. Son temiz yeniden yükleme sonrasında yeni tarayıcı warning/error kaydı oluşmadı. İzole `IK_E2E_CARRY_REVIEW` veritabanı doğrulama sonrası kaldırıldı.
- Odaklı birleşik karar/worker/filtre ve servis testleri 52/52, tam Release paketi 163/163 geçti; Release build sıfır uyarı/sıfır hata ve `git diff --check` temiz tamamlandı. Kaynak bazlı allocation tabanı, incelenmiş kararın otomatik uzlaştırmada korunması, tek UI otoritesi ve gerçek filtre temizleme sözleşmeyle kanıtlandı. Gerçek ilişkisel SQL testi, eski satır sürümüyle ilk onayın reddedildiğini ve aynı servis örneğinde taze sürümle yeniden denemenin tek audit kaydıyla başarıyla tamamlandığını doğruladı. Değişiklik yeni şema veya migration gerektirmedi.
